# Copy to ".env" in this same folder.
# This is the only value the public site needs — the URL of your API.
# Locally, this matches the PORT your api/.env is running on.
# Once deployed, point it at your real API domain, e.g. https://api.blooom.online
VITE_API_URL=http://localhost:4000
